version: '3.7'
services:
  scanner:
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes:
      - ./data:/data
    environment:
      PYTHONUNBUFFERED: 1
      INPUT_FILE: "city_councils_small.csv"
